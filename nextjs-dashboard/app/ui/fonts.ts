import { Inter, Lusitana, Arima } from 'next/font/google';

export const inter = Inter({ subsets: ['latin']});
export const lusitana = Lusitana({ subsets: ['latin'], weight:['400', '700']});
export const arima = Arima({ subsets: ['latin'], weight:['400', '700']});